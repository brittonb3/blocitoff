<% if @item.destroyed? %>
   $('#item-<%=@item.id %>').hide();
   $('.notice').text("Item was deleted successfully.");
 <% else %>
   $('.flash').text("Item couldn't be deleted. Please try again.");
 <% end %>
